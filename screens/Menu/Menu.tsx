import * as React from 'react'
import MenuLayout from '../../components/MenuLayout';

const Menu = ({...props}: any) => {
    return (
        <MenuLayout>
            <div>Menu</div>
        </MenuLayout>
    );
}

export default Menu
